# Copy to .env and adjust values for your server.
# For local quick start you can keep defaults from docker-compose.

DB_USER=applymanager
DB_PASSWORD=change_me_strong_password
DB_NAME=applymanager

# Optional explicit Postgres vars (when omitted, they fallback to DB_* values)
# POSTGRES_USER=applymanager
# POSTGRES_PASSWORD=change_me_strong_password
# POSTGRES_DB=applymanager
